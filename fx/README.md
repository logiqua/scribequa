# Logiqua Interactive GUI Playground

This module implements a simple GUI for Logiqua.

![Logiqua screenshot](screenshot.png)

You select the format you want to evaluate (default is JSON),
type in the expression,
type the data (optional) and press "Run".

The result will be displayed in the bottom pane.

If there is an error the exception will be displayed in red.